name = "webcomic-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# ADMIN_ORIGIN is set via .dev.vars locally and wrangler secret in production
# All sensitive vars should be in .dev.vars (local) or secrets (production)

# For local development, create a .dev.vars file with:
# SANITY_PROJECT_ID=your_project_id
# SANITY_DATASET=production
# SANITY_WRITE_TOKEN=your_write_token
